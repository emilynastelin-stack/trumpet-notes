---
--- 
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #1f1c2c, #928dab);
        color: white;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .container {
        text-align: center;
        padding: 2rem 3rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 1rem;
        box-shadow: 0 8px 20px rgba(0,0,0,0.3);
      }

      h1 {
        font-size: 2.5rem;
        margin-bottom: 2rem;
      }

      a.button {
        display: inline-block;
        padding: 1rem 2rem;
        font-size: 1.2rem;
        font-weight: bold;
        text-decoration: none;
        color: white;
        border-radius: 50px;
        transition: transform 220ms ease, box-shadow 220ms ease, filter 220ms ease;
        box-shadow: 0 6px 18px rgba(2,6,23,0.28);
        width: 220px;
        text-align: center;
      }

      a.button:hover {
        transform: translateY(-6px) scale(1.02);
        box-shadow: 0 18px 54px rgba(0,0,0,0.45), 0 0 20px rgba(59,130,246,0.06);
        filter: brightness(1.05) saturate(1.05);
      }

      /* Refined color palette classes (softer, modern gradients) */
      .btn-beginner { background: linear-gradient(90deg,#2dd4bf,#60a5fa); border: 1px solid rgba(255,255,255,0.06); }
      .btn-intermediate { background: linear-gradient(90deg,#fbbf24,#fb923c); border: 1px solid rgba(255,255,255,0.06); }
      .btn-advanced { background: linear-gradient(90deg,#7c3aed,#a78bfa); border: 1px solid rgba(255,255,255,0.06); }
      .btn-speed { background: linear-gradient(90deg,#34d399,#059669); border: 1px solid rgba(255,255,255,0.06); }

      /* subtle glowing accent on focus/hover for accessibility */
      a.button:focus { outline: none; box-shadow: 0 8px 30px rgba(59,130,246,0.12); }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Trumpet Notes</h1>
    <div style="display:flex; flex-direction:column; gap:12px; justify-content:center; align-items:center; height:60vh;">
  <a href="/Beginner" class="button btn-beginner">Beginner</a>
  <a href="/Intermediate" class="button btn-intermediate">Intermediate</a>
  <a href="/FingeringBasics" class="button btn-advanced">Advanced</a>
  <a href="/SpeedTest/" class="button btn-speed">Speed Test</a>
  <a href="/Marathon/" class="button btn-intermediate">Marathon</a>
    </div>
    </div>
  </body>
</html>
