---
--- 
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #1f1c2c, #928dab);
        color: white;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .container {
        text-align: center;
        padding: 2rem 3rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 1rem;
        box-shadow: 0 8px 20px rgba(0,0,0,0.3);
      }

      h1 {
        font-size: 2.5rem;
        margin-bottom: 2rem;
      }

      a.button {
        display: inline-block;
        padding: 1rem 2rem;
        font-size: 1.2rem;
        font-weight: bold;
        text-decoration: none;
        color: white;
        border-radius: 50px;
        transition: transform 220ms ease, box-shadow 220ms ease, filter 220ms ease;
        box-shadow: 0 6px 18px rgba(2,6,23,0.28);
        width: 220px;
        text-align: center;
      }

      a.button:hover {
        transform: translateY(-4px) scale(1.01);
        box-shadow: 0 14px 40px rgba(2,6,23,0.32);
        filter: brightness(1.02);
      }

      /* Color palette classes */
      .btn-beginner { background: linear-gradient(90deg,#06b6d4,#3b82f6); } /* teal -> blue */
      .btn-intermediate { background: linear-gradient(90deg,#f59e0b,#f97316); } /* amber -> orange */
      .btn-advanced { background: linear-gradient(90deg,#7c3aed,#8b5cf6); } /* indigo -> violet */
      .btn-speed { background: linear-gradient(90deg,#10b981,#059669); } /* emerald -> green */
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Trumpet Notes</h1>
      <div style="display:flex; flex-direction:column; gap:12px; justify-content:center; align-items:center;">
  <a href="/Beginner" class="button btn-beginner">Beginner</a>
  <a href="/Intermediate" class="button btn-intermediate">Intermediate</a>
  <a href="/FingeringBasics" class="button btn-advanced">Advanced</a>
  <a href="/SpeedTest/" class="button btn-speed">Speed Test</a>
      </div>
    </div>
  </body>
</html>
